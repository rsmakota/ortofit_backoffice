<!-- Modal -->
<div class="modal-dialog" role="document">
    <div class="modal-content">
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span
                        aria-hidden="true">&times;</span></button>
            <h4 class="modal-title" id="myModalLabel">Заключение</h4>
            <div style="font-weight: bold; padding-top: 10px; text-align: center ">
                Визит был успешно оформлен! <br> Завершите оформнение или оформите еще одного члена семьи.
            </div>
        </div>

        <div class="modal-footer">
            <form class="form-horizontal" id="rewindForm">
                <input type="hidden" id="appId"    name="appId"    value="{{ app.id }}"   >

                <button type="button" class="btn btn-info"    id="rewindButton"><i class="fa fa-reply-all"></i> Оформить еще</button>
                <button type="button" class="btn btn-success" id="nextButton"  >Завершить</button>

            </form>
        </div>
    </div>
</div>
<script>
    $(document).ready(function() {
        var control = BackOffice.OrderRewind;
        control.postUrl = "{{ url('bf_app_order_process') }}";
        control.appId   = {{ app.id }};
        control.init();
    });
</script>